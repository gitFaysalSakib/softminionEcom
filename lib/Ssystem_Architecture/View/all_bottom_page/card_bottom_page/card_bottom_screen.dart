import 'package:flutter/material.dart';

class CardBottomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Card Screen'),
    );
  }
}
