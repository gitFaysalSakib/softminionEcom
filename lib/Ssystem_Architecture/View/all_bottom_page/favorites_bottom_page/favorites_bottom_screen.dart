import 'package:flutter/material.dart';

class FavoritesBottomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Categories Screen'),
    );
  }
}
